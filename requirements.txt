fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0
pydantic[email]>=2.10
python-multipart>=0.0.20
psycopg2-binary>=2.9   # Postgres driver — used automatically when DATABASE_URL is set (e.g. on Render)
itsdangerous>=2.1       # signed cookie sessions for staff login (Starlette SessionMiddleware dependency)
boto3>=1.34             # cloud backup upload (S3-compatible: AWS S3, Backblaze B2, Cloudflare R2, DO Spaces)
reportlab>=4.0          # server-side PDF generation for downloadable / emailable operations reports
